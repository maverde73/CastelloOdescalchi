<Window x:Class="Presentation.CustomControls.ucLogin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             Title="SCV"
		>
             



    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>




        <StackPanel Grid.Column="0" Margin="0 30 0 0">
            <TextBlock Text="Utente:" Margin="8 8 8 0" Foreground="#FF393838" />
            <TextBlock Text="Password:" Margin="8 24 8 0" Foreground="#FF393838" />
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="0 30 0 0">
            <TextBox Name="txtUserName" Margin="8" Height="25"></TextBox>
            <PasswordBox Name="pwdPassword" Margin="8" Height="25" />
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
				VerticalAlignment="Center" Margin="8">
            <telerik:RadButton Content="Entra"  Width="60" Margin="0 0 10 0" Click="RadButton_Click" />

            <telerik:RadButton Content="Annulla" Command="{Binding CancelCommand}"
					CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Width="60" Margin="0 0 5 0" />
        </StackPanel>
    </Grid>
</Window>
